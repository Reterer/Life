# -*- coding: utf-8 -*-

class Food:
	def __init__(self, x, y, radius, color, energy):
		self.x = x
		self.y = y
		self.radius = radius
		self.color = color
		self.energy = energy
		self.is_alive = True
